task allCompile << {
	println 'All compiled'
}
//allCompile.dependsOn(project.tasks.findAll {
//	it.name.startsWith('compile')
//})
//allCompile.dependsOn(project.tasks.matching {
//	it.name.startsWith('compile')
//})
allCompile.dependsOn { 
	project.tasks.findAll {
		it.name.startsWith('compile')
	}
}

5.times {
	task "compile${it}" << { task -> println "${task.name}" }
}


task compileOther << { println it.name }