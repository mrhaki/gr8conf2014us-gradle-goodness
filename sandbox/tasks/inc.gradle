version = '1.1'

task createVersionFile {
	ext {
		outputFile = project.file('VERSION')
	}

	inputs.property 'version', project.version
	outputs.file outputFile

	doFirst {
		outputFile.text = version
	}
}